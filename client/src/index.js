// initiate React application
import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

// target root element inside of html
// pass in App component
// every thing create in React --> inside of App --> put inside of root
ReactDOM.render(<App />, document.querySelector("#root"));

// Boiler plate code for every React
// -----------------------------
